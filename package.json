{
  "name": "khamel-redirector",
  "version": "1.0.0",
  "description": "URL shortener/redirector for Tailscale Funnel endpoints",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
